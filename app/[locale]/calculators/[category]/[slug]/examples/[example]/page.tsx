import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import AdPlaceholder from "@/components/ads/AdPlaceholder";
import AdSlot from "@/components/ads/AdSlot";
import CalculatorEngine from "@/components/calculators/CalculatorEngine";
import { calculatorCategories, calculators, calculatorsByCategory, getCalculator, getRelatedCalculators } from "@/lib/calculators/data";
import { computeCalculator, getDefaultValues } from "@/lib/calculators/engine";
import { getCalculatorExamples, getFormulaSeo, getGuideSeo, getProgrammaticHubLinks, getUseCases } from "@/lib/calculators/programmatic";
import { normalizeLocale, withLocale } from "@/lib/i18n";
import { calculatorKeywordLine, localizeCalculatorDefinition, localizeCalculatorName, localizeCategoryName, localizeDescription, localizeDisplayValue, localizeInputLabel, localizeProgrammaticExample, localizeResultText, localizeUiText } from "@/lib/calculators/localization";

export function generateStaticParams() { return calculators.flatMap((calculator) => getCalculatorExamples(calculator).flatMap((example) => ([{ locale: "en", category: calculator.category, slug: calculator.slug, example: example.slug }, { locale: "ko", category: calculator.category, slug: calculator.slug, example: example.slug }]))); }
export async function generateMetadata({ params }: { params: Promise<{ locale: string; category: string; slug: string; example: string }> }): Promise<Metadata> { const { locale: rawLocale, category, slug, example } = await params; const locale = normalizeLocale(rawLocale); const calculator = getCalculator(category, slug); if (!calculator) return {}; const rawExampleData = getCalculatorExamples(calculator).find((item) => item.slug === example); if (!rawExampleData) return {}; const exampleData = localizeProgrammaticExample(rawExampleData, calculator, locale); return { title: locale === "ko" ? `${exampleData.title} | 예시` : `${exampleData.title} | Mega Calculators`, description: locale === "ko" ? exampleData.description : exampleData.description, alternates: { canonical: `/${locale}/calculators/${calculator.category}/${calculator.slug}/examples/${exampleData.slug}`, languages: { en: `/en/calculators/${calculator.category}/${calculator.slug}/examples/${exampleData.slug}`, ko: `/ko/calculators/${calculator.category}/${calculator.slug}/examples/${exampleData.slug}` } } }; }
export default async function CalculatorExamplePage({ params }: { params: Promise<{ locale: string; category: string; slug: string; example: string }> }) { const { locale: rawLocale, category, slug, example } = await params; const locale = normalizeLocale(rawLocale); const calculator = getCalculator(category, slug); if (!calculator) notFound(); const localized = localizeCalculatorDefinition(calculator, locale); const rawExampleData = getCalculatorExamples(calculator).find((item) => item.slug === example); if (!rawExampleData) notFound(); const exampleData = localizeProgrammaticExample(rawExampleData, calculator, locale); const values = { ...getDefaultValues(calculator), ...exampleData.overrides }; const result = computeCalculator(calculator, values, locale); const hubLinks = getProgrammaticHubLinks(calculator); const isKo = locale === "ko"; const schema = { "@context": "https://schema.org", "@type": "Article", headline: exampleData.title, description: isKo ? `${localized.name} 예시 계산` : exampleData.description, mainEntityOfPage: `https://mega-calculators.com/${locale}/calculators/${calculator.category}/${calculator.slug}/examples/${exampleData.slug}`, inLanguage: locale }; return <div className="space-y-8"><script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(schema) }} /><nav className="text-sm text-slate-500"><Link href={withLocale(locale, "/")}>{isKo ? "홈" : "Home"}</Link><span className="mx-2">/</span><Link href={withLocale(locale, "/calculators")}>{isKo ? "계산기" : "Calculators"}</Link><span className="mx-2">/</span><Link href={withLocale(locale, `/calculators/${calculator.category}`)}>{localizeCategoryName(calculator.category, locale)}</Link><span className="mx-2">/</span><Link href={withLocale(locale, `/calculators/${calculator.category}/${calculator.slug}`)}>{localized.name}</Link></nav><section className="rounded-[2rem] border border-slate-200 bg-white p-8 shadow-sm"><div className="text-sm font-semibold uppercase tracking-[0.2em] text-blue-700">{isKo ? "예시 계산" : "Example calculation"}</div><h1 className="mt-2 text-4xl font-black tracking-tight text-slate-950">{exampleData.title}</h1><p className="mt-4 max-w-4xl text-base leading-8 text-slate-600">{exampleData.intro}</p></section><section className="grid gap-6 lg:grid-cols-[0.95fr_1.05fr]"><article className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm md:p-8"><h2 className="text-2xl font-bold text-slate-900">{isKo ? "이 예시에 사용된 입력값" : "Inputs used in this example"}</h2><div className="mt-5 space-y-3">{localized.inputs.map((input) => <div key={input.name} className="flex items-center justify-between gap-4 rounded-2xl bg-slate-50 px-4 py-3"><span className="text-sm text-slate-600">{input.label}</span><span className="text-sm font-semibold text-slate-900">{locale === "ko" ? localizeDisplayValue(input.label, String(values[input.name] ?? "—"), locale) : values[input.name] ?? "—"}</span></div>)}</div></article><article className="rounded-3xl border border-slate-200 bg-slate-950 p-6 text-white shadow-sm md:p-8"><div className="text-sm font-semibold uppercase tracking-[0.2em] text-blue-300">{isKo ? "예상 결과" : "Estimated result"}</div><div className="mt-5 rounded-3xl bg-white/5 p-6"><div className="text-sm text-slate-300">{localizeResultText(result.primary.label, locale)}</div><div className="mt-3 text-3xl font-extrabold tracking-tight text-white md:text-4xl">{localizeResultText(result.primary.value, locale)}</div></div>{result.secondary?.length ? <div className="mt-6 space-y-3">{result.secondary.map((item) => <div key={`${item.label}-${item.value}`} className="flex items-center justify-between gap-4 rounded-2xl border border-white/10 bg-white/5 px-4 py-3"><span className="text-sm text-slate-300">{localizeResultText(item.label, locale)}</span><span className="text-sm font-semibold text-white">{localizeResultText(item.value, locale)}</span></div>)}</div> : null}{result.note ? <p className="mt-5 text-sm leading-7 text-amber-200">{localizeResultText(result.note, locale)}</p> : null}</article></section><AdSlot slotKey="contentMid" label="Example page ad" minHeightClass="min-h-[140px]" /><section className="grid gap-6 lg:grid-cols-2"><article className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm md:p-8"><h2 className="text-2xl font-bold text-slate-900">{isKo ? "이 예시가 도움이 되는 이유" : "Why this example matters"}</h2><p className="mt-4 text-sm leading-7 text-slate-600 md:text-base">{isKo ? "예시 페이지는 처음 보는 계산기에서도 어떤 숫자를 넣어야 하는지 감을 잡기 쉽게 해줍니다. 빈 화면에서 시작하는 부담을 줄여주는 역할을 합니다." : "Example pages help you understand a calculator faster because they remove the blank-screen problem. Instead of guessing which numbers to enter, you can review a practical scenario and then adjust the values to match your own needs."}</p><p className="mt-4 text-sm leading-7 text-slate-600 md:text-base">{isKo ? "특정 금액이나 조건으로 검색해 들어오는 사용자가 바로 결과 감을 볼 수 있도록 구성했습니다." : "This is especially useful for users who searched for a very specific long-tail question and want a quick answer before opening the full interactive tool."}</p></article><article className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm md:p-8"><h2 className="text-2xl font-bold text-slate-900">{isKo ? "관련 페이지 이동" : "Continue exploring"}</h2><div className="mt-4 space-y-3"><Link href={withLocale(locale, `/calculators/${calculator.category}/${calculator.slug}`)} className="block rounded-2xl bg-slate-50 px-4 py-3 text-sm font-semibold text-slate-700 hover:bg-blue-50 hover:text-blue-700">{isKo ? "계산기 열기" : "Open calculator"}</Link><Link href={withLocale(locale, hubLinks.formula)} className="block rounded-2xl bg-slate-50 px-4 py-3 text-sm font-semibold text-slate-700 hover:bg-blue-50 hover:text-blue-700">{isKo ? "공식 보기" : "Read the formula"}</Link><Link href={withLocale(locale, hubLinks.guide)} className="block rounded-2xl bg-slate-50 px-4 py-3 text-sm font-semibold text-slate-700 hover:bg-blue-50 hover:text-blue-700">{isKo ? "가이드 보기" : "Read the guide"}</Link></div></article></section></div>; }
