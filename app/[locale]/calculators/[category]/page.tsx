import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import AdPlaceholder from "@/components/ads/AdPlaceholder";
import AdSlot from "@/components/ads/AdSlot";
import CalculatorEngine from "@/components/calculators/CalculatorEngine";
import { calculatorCategories, calculators, calculatorsByCategory, getCalculator, getRelatedCalculators } from "@/lib/calculators/data";
import { computeCalculator, getDefaultValues } from "@/lib/calculators/engine";
import { getCalculatorExamples, getFormulaSeo, getGuideSeo, getProgrammaticHubLinks, getUseCases } from "@/lib/calculators/programmatic";
import { normalizeLocale, withLocale } from "@/lib/i18n";
import { calculatorKeywordLine, localizeCalculatorDefinition, localizeCalculatorName, localizeCategoryName, localizeDescription, localizeUiText } from "@/lib/calculators/localization";

export function generateStaticParams() { return calculatorCategories.flatMap((category) => ([{ locale: "en", category: category.slug }, { locale: "ko", category: category.slug }])); }
export async function generateMetadata({ params }: { params: Promise<{ locale: string; category: string }> }): Promise<Metadata> { const { locale: rawLocale, category } = await params; const locale = normalizeLocale(rawLocale); const match = calculatorsByCategory.find((item) => item.slug === category); if (!match) return {}; const name = localizeCategoryName(category, locale); return { title: locale === "ko" ? `${name} 모음` : `${match.name} - Free Online Tools`, description: locale === "ko" ? `${name}를 한 페이지에서 모아볼 수 있습니다. 관련 계산기를 비교하고 바로 사용할 수 있습니다.` : `Browse ${match.items.length} free ${match.name.toLowerCase()} on Mega Calculators.`, alternates: { canonical: `/${locale}/calculators/${match.slug}`, languages: { en: `/en/calculators/${match.slug}`, ko: `/ko/calculators/${match.slug}` } } }; }
export default async function CategoryPage({ params }: { params: Promise<{ locale: string; category: string }> }) { const { locale: rawLocale, category } = await params; const locale = normalizeLocale(rawLocale); const match = calculatorsByCategory.find((item) => item.slug === category); if (!match) notFound(); const items = match.items.map((item) => localizeCalculatorDefinition(item, locale)); const name = localizeCategoryName(category, locale); const isKo = locale === "ko"; const categorySchema = { "@context": "https://schema.org", "@type": "CollectionPage", name, description: isKo ? `${name} 모음 페이지` : `${match.name} on Mega Calculators.`, url: `https://mega-calculators.com/${locale}/calculators/${match.slug}`, inLanguage: locale }; return <div className="space-y-8"><script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(categorySchema) }} /><section className="rounded-[2rem] bg-white p-8 shadow-sm"><div className="text-sm font-semibold uppercase tracking-[0.2em] text-blue-700">{isKo ? "카테고리" : "Category"}</div><h1 className="mt-2 text-4xl font-black tracking-tight text-slate-950">{name}</h1><p className="mt-4 max-w-4xl text-base leading-8 text-slate-600">{isKo ? `${name}에 포함된 ${items.length}개 도구를 한 번에 둘러볼 수 있습니다. 각 페이지에는 계산 기능과 함께 한국어 설명, FAQ, 예시 페이지가 연결됩니다.` : `Explore ${items.length} free tools in the ${match.name.toLowerCase()} section. Each page includes a working calculator, a short explanation, usage steps, and related tools you may also find helpful.`}</p></section><AdPlaceholder label="Category ad" /><section className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{items.map((item) => <Link key={`${item.category}-${item.slug}`} href={withLocale(locale, `/calculators/${item.category}/${item.slug}`)} className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm transition hover:border-blue-200 hover:bg-blue-50"><div className="text-lg font-bold text-slate-950">{item.name}</div><p className="mt-3 text-sm leading-7 text-slate-600">{item.description}</p></Link>)}</section><section className="grid gap-6 lg:grid-cols-2"><article className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm md:p-8"><h2 className="text-2xl font-bold text-slate-900">{isKo ? "이 카테고리에서 할 수 있는 것" : "What you can do here"}</h2><ul className="mt-4 space-y-3 text-sm leading-7 text-slate-600 md:text-base"><li className="rounded-2xl bg-slate-50 px-4 py-3">{isKo ? "비슷한 계산기를 한 번에 비교할 수 있습니다." : "Compare related tools without opening multiple tabs."}</li><li className="rounded-2xl bg-slate-50 px-4 py-3">{isKo ? `${name} 관련 계산을 빠르게 찾을 수 있습니다.` : `Find quick answers for common questions in ${match.name.toLowerCase()}.`}</li><li className="rounded-2xl bg-slate-50 px-4 py-3">{isKo ? "예시와 가이드 페이지까지 연결해 이해를 도와줍니다." : "Move between similar calculators and check examples when needed."}</li></ul></article><article className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm md:p-8"><h2 className="text-2xl font-bold text-slate-900">{isKo ? "이 카테고리의 대표 계산기" : "Popular picks in this category"}</h2><div className="mt-4 space-y-3">{items.slice(0, 4).map((item) => <Link key={`featured-${item.category}-${item.slug}`} href={withLocale(locale, `/calculators/${item.category}/${item.slug}`)} className="block rounded-2xl bg-slate-50 px-4 py-3 text-sm font-semibold text-slate-700 hover:bg-blue-50 hover:text-blue-700">{item.name}</Link>)}</div></article></section></div>; }
